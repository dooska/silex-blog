{% extends 'layout.twig' %}

{% block title %}{{  'article_add'|trans }}{% endblock %}

{% block content %}
    <h1>
        {{ 'article_add'|trans }}
    </h1>
    <form action="{{ url('articles_add') }}" method="post">
        {{ form_widget(form) }}
        <input type="submit" name="submit" class="btn btn-primary" value="{{ 'save'|trans }}" />
    </form>
{% endblock %}